spring:
  application:
    name: Seata-Business

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        ext-config[0]:
          data-id: Seata-Test-Common.yml
          refresh: true